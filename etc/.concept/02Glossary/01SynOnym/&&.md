- && (앤드)	성공 시에만 실행	git add .가 성공했을 때만 git commit을 실행합니다. 보통 이게 더 안전합니다.
- ; (세미콜론) vs && (앰퍼샌드)
- && (앰퍼샌드)
- 사용 가능 여부만 따지면 ;도 충분히 가능합니다! 하지만 개발자들 사이에서는 보통 &&를 쓰는 것이 국룰(Best Practice)로 통합니다.
- &&
- `&&`
- `;`.`&&`